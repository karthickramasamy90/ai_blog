/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Disable strict mode (optional)
  // REMOVE appDir to force Pages Router
  experimental: {},
};

module.exports = nextConfig;
